# CLAUDE.md

本文档为 Claude Code 在本仓库工作时提供指导。

## 项目概述

git-browser 是一个极简的 Git 图形化桌面客户端，专注于代码浏览和日常 Git 操作。项目遵循简洁优先的设计理念：只满足日常开发 80% 的常见需求，不追求覆盖所有 Git 高级功能。

目前处于**设计阶段** - UI 规范已完成，尚未开始实现。

## 仓库结构

```
git-browser/
├── CLAUDE.md              # 本文档 - Claude Code 开发指南
├── README.md              # 极简项目描述
├── .gitignore             # Git 忽略配置 (Node.js/npm 模式)
└── ui-design/             # UI 设计文档和资源
│   ├── design.md          # 完整 UI 设计规范 (中文)
│   ├── design.eddx        # Enterprise Architect 设计文件
│   └── assets/            # UI 设计截图
```

## 设计架构

### 布局
采用三段式布局：
- **顶部标题栏**：设置按钮、项目名称 + 当前分支、文件路径、关闭按钮
- **主体内容区**：左侧 - 文件目录树，右侧 - 代码浏览器（分隔条支持拖动调整宽度）
- **底部操作区**：命令输入框或操作按钮

### 核心功能（来自设计规范）
1. **文件浏览**：支持大型仓库的虚拟化树视图、模糊搜索、状态颜色标记
2. **代码浏览**：支持 10+ 种语言语法高亮、行号显示、文件内搜索
3. **两种工作模式**：
   - 命令模式：直接输入 Git 命令，执行结果反馈
   - 按钮模式：常用操作预设按钮（切换分支、拉取、推送、合并、对比）
4. **Git 功能**：提交、分支管理、Diff 查看、冲突解决、历史浏览
5. **设置**：加密凭证存储（SSH/HTTP）、显示选项、主题切换
6. **状态可视化**：文件状态颜色编码：绿色（新增）、红色（删除）、蓝色（修改）、橙色（冲突）

### 技术设计原则
- 性能：大文件懒加载、目录树虚拟化渲染
- 安全：加密凭证存储、危险操作二次确认
- 跨平台：目标为桌面应用程序

## 预期技术实现

根据项目性质，预期实现技术栈：
- **运行时**：基于 Electron 的桌面应用
- **包管理器**：npm/yarn（Node.js 项目）
- **Git 集成**：NodeGit 或 isomorphic-git
- **UI 框架**：现代前端框架（React/Vue/Svelte）
- **语法高亮**：Prism.js 或类似库
- **构建工具**：Vite 或 webpack + Electron Builder

## 常用命令

目前尚未配置构建系统。当实现完成后：
- 安装依赖：`npm install` 或 `yarn install`
- 开发模式运行：`npm run dev` 或 `yarn dev`
- 构建应用：`npm run build` 或 `yarn build`
- 运行测试：`npm test` 或 `yarn test`

## 设计文档

所有 UI/UX 需求都记录在 `ui-design/design.md`（中文）中。开始实现前请阅读该文件以了解完整规范。
